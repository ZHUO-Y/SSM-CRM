1.登录拦截问题。
    写了一个拦截器，想要拦截恶意登陆，发现无法拦截，根本就不进入拦截器。
    拦截器失效？不是，正常登录的时候，是进入拦截器，然后放行，说明拦截器并没有失效。
    后来查看springmvc的配置里，在web.xml中配置中央调度器时，全局拦截选用的是*.do，
    只有请求会进入mvc，所以拦截器压根不拦截.jsp文件的访问，也就无法拦截恶意登录。
    解决方法：换成过滤器，或者全局拦截设置为/*，拦截全部文件。但是需要管理静态资源。

2.ajax执行顺序的问题。
    ajax的默认设置是异步的，这导致有的时候代码还没结束，窗口信息的展示已经结束了，信息没有展现出来。
    解决方法，设置为同步，或者把展现窗口写在success里面。

3.limit在部分sql版本中是不支持的。

4.jQuery引入问题，未知原因

5.sql命令写错！！！查询不出市场活动列表总数，导致分页ui控件显示不全。

6.配置文件漏写。

7.